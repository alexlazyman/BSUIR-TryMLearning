(function () {
    'use strict';

    angular
        .module('app')
        .controller('estimationAllCtrl', estimationAllCtrl);

    estimationAllCtrl.$inject = [
    ];

    function estimationAllCtrl(
    ) {
    }
})();
