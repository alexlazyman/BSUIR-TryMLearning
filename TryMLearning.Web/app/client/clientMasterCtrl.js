(function () {
    'use strict';

    angular
        .module('app')
        .controller('clientMasterCtrl', clientMasterCtrl);

    clientMasterCtrl.$inject = [
    ];

    function clientMasterCtrl(
    ) {
        var vm = this;

        function activate() {
        }
    }
})();
