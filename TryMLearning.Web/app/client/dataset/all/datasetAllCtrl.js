(function () {
    'use strict';

    angular
        .module('app')
        .controller('datasetAllCtrl', datasetAllCtrl);

    datasetAllCtrl.$inject = [
    ];

    function datasetAllCtrl(
    ) {
    }
})();
